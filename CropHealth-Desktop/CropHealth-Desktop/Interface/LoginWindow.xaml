<Window x:Class="CropHealth_Desktop.Interface.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Login" Height="720" Width="500" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="#BDC3C7" BorderThickness="2" CornerRadius="8" Background="White">
                            <Border.Effect>
                                <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.1"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,10"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border BorderBrush="#BDC3C7" BorderThickness="2" CornerRadius="8" Background="White">
                            <Border.Effect>
                                <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.1"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,10"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495e"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="4" BlurRadius="8" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#764ba2" Offset="0"/>
                                            <GradientStop Color="#667eea" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#f5f7fa" Offset="0"/>
                <GradientStop Color="#c3cfe2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border Background="White" 
                CornerRadius="16" 
                BorderThickness="0"
                Margin="30"
                Padding="40,35"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="8" BlurRadius="20" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                          Text="🌾 Crop Health Advisor" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="#2c3e50"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" 
                          Text="Admin Login" 
                          FontSize="18" 
                          Foreground="#7f8c8d"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,30"/>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <Label Content="Email:"/>
                    <TextBox x:Name="EmailTextBox"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Margin="0,0,0,25">
                    <Label Content="Parola:"/>
                    <PasswordBox x:Name="PasswordBoxControl"/>
                </StackPanel>

                <Button Grid.Row="4" 
                        Content="Login" 
                        Click="LoginButton_Click" 
                        Width="200"
                        HorizontalAlignment="Center"/>

                <TextBlock x:Name="ErrorTextBlock" 
                          Grid.Row="5" 
                          Foreground="#e74c3c" 
                          Margin="0,15,0,0" 
                          TextWrapping="Wrap"
                          FontSize="13"
                          HorizontalAlignment="Center"
                          TextAlignment="Center"
                          Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Window>
