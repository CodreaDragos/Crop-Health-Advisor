<Window x:Class="CropHealth_Desktop.Interface.LocationFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CropHealth_Desktop.Interface"
        mc:Ignorable="d"
        Title="Adaugă/Editează Locație" Height="500" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" 
                   Text="Adaugă Locație Nouă" 
                   FontSize="20" FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Form -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Label Content="Nume Locație:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox" Padding="8" Margin="0,0,0,15"/>

            <Label Content="Latitudine:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="LatitudeTextBox" Padding="8" Margin="0,0,0,15"/>

            <Label Content="Longitudine:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="LongitudeTextBox" Padding="8" Margin="0,0,0,15"/>

            <Label Content="Utilizator:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="UserComboBox" Padding="8" Margin="0,0,0,15" DisplayMemberPath="Username"/>

            <TextBlock x:Name="ErrorTextBlock" 
                      Foreground="Red" 
                      TextWrapping="Wrap"
                      Margin="0,10,0,0"
                      Visibility="Collapsed"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button Content="Salvează" Click="SaveButton_Click" 
                    Padding="15,8" Margin="0,0,10,0"
                    Background="#667eea" Foreground="White" 
                    FontWeight="Bold" MinWidth="100"/>
            <Button Content="Anulează" Click="CancelButton_Click" 
                    Padding="15,8" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>

